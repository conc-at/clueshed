<%= form_for(@contrib) do |f| %>
  <% if @contrib.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contrib.errors.count, "error") %> prohibited this contrib from being saved:</h2>

      <ul>
      <% @contrib.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
